namespace AccServerAdmin.Service
{
    /// <summary>
    /// TODO: research how the needs configuring value can be stored and the menu partial able to retrieve it to build the menu correctly
    /// </summary>
    public sealed class Globals
    {
        public static bool NeedsConfiguring { get; set; }
    }
}
